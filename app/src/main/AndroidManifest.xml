<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    package="com.example.githubuserexplorer">

    <!-- 🔐 Internet permission for Retrofit, Glide, etc. -->
    <uses-permission android:name="android.permission.INTERNET" />

    <application
        android:icon="@mipmap/ic_launcher"
        android:allowBackup="true"
        android:label="GitHub Explorer"
        android:supportsRtl="true"
        android:theme="@style/Theme.GitHubUserExplorer">  <!-- ✅ Updated theme -->

        <!-- 🚀 Launcher Activity -->
        <activity
            android:name=".MainActivity"
            android:exported="true">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />
                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>

        <!-- 👥 Followers and Following Views -->
        <activity android:name=".FollowersActivity" />
        <activity android:name=".FollowingActivity" />
        <activity android:name=".UserProfileActivity" />
    </application>

</manifest>
